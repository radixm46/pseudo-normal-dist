// generate random numbers
// call this from main.html

function main {
  // make 13 integers sum 100
  const listLength = 100;
  const numberOfGroups = 13;
  var distributionList = new Int8Array(listLength);

  // enter loop of distributionList
  // use for loop, generate 12 len bool tuple
  for (const int8elem in distributionList) {
    //hoge;
    var additionalVals = new Int8Array(numberOfGroups);
    for (const addelem in additionalVals) {
      // generate list of bool, use for additional value
      // use random here
    }
    // add sum of additionalVals to distributionlist
  }
  return true;
  // count elements

}
